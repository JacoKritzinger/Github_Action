name: Github Action Demo

on:
 push:
    branches: [ "main" ]
 pull_request:
    branches: [ "main" ]    
schedule:
    -cron: "15 6 * * sun"
    
# paths-ignore:
#       - .github/**
workflow_dispatch:
 
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: "the job tat triggered whas $Github"

   
